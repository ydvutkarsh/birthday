@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">The main character</p><h1>Birthday <em>profile</em></h1><p class="muted">Set the details that introduce her to this little world.</p></div></div>@include('admin.partials.flash')
<form method="POST" action="{{ route('admin.profile.update') }}" enctype="multipart/form-data" class="studio-card form-card">@csrf @method('PUT')<div class="row g-4"><div class="col-md-6"><label>Name</label><input name="name" value="{{ old('name',$profile->name) }}" required><label>Nickname</label><input name="nickname" value="{{ old('nickname',$profile->nickname) }}"><label>Birthday date</label><input type="date" name="birthday_date" value="{{ old('birthday_date',$profile->birthday_date?->format('Y-m-d')) }}"></div><div class="col-md-6"><label>Hero title</label><input name="hero_title" value="{{ old('hero_title',$profile->hero_title) }}" required><label>Hero subtitle</label><input name="hero_subtitle" value="{{ old('hero_subtitle',$profile->hero_subtitle) }}"><label>Intro message</label><textarea name="intro_message" rows="4">{{ old('intro_message',$profile->intro_message) }}</textarea></div><div class="col-md-6"><label>Profile image <small>JPG, PNG, WEBP · max 5MB</small></label><input type="file" name="profile_image" accept="image/*">@if($profile->profile_image)<img class="form-preview" src="{{ asset('storage/'.$profile->profile_image) }}">@endif</div><div class="col-md-6"><label>Cover image <small>JPG, PNG, WEBP · max 5MB</small></label><input type="file" name="cover_image" accept="image/*">@if($profile->cover_image)<img class="form-preview" src="{{ asset('storage/'.$profile->cover_image) }}">@endif</div></div><button class="btn btn-dark-gold mt-4">Save profile</button></form>@endsection
