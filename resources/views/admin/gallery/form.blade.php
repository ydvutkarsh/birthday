@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">{{ $photo->exists ? 'Refine a frame' : 'A new frame' }}</p><h1>{{ $photo->exists ? 'Edit' : 'Upload' }} <em>photo</em></h1></div></div>@include('admin.partials.flash')<form method="POST" action="{{ $photo->exists ? route('admin.gallery.update',$photo) : route('admin.gallery.store') }}" enctype="multipart/form-data" class="studio-card form-card">@csrf @if($photo->exists) @method('PUT') @endif<label>Image <small>JPG, PNG, WEBP · max 5MB</small></label><input type="file" name="image" accept="image/*" {{ $photo->exists ? '' : 'required' }}>@if($photo->image)<img class="form-preview wide-preview" src="{{ asset('storage/'.$photo->image) }}">@endif<label>Title</label><input name="title" value="{{ old('title',$photo->title) }}"><label>Caption</label><input name="caption" value="{{ old('caption',$photo->caption) }}"><div class="row g-3"><div class="col-md-6"><label>Memory date</label><input type="date" name="memory_date" value="{{ old('memory_date',$photo->memory_date?->format('Y-m-d')) }}"></div><div class="col-md-6"><label>Display order</label><input type="number" name="sort_order" min="0" value="{{ old('sort_order',$photo->sort_order ?? 0) }}"></div></div><label class="check-line"><input type="checkbox" name="is_featured" value="1" @checked(old('is_featured',$photo->is_featured))> <span>Feature this photo</span></label><button class="btn btn-dark-gold mt-4">{{ $photo->exists ? 'Save changes' : 'Upload photo' }}</button> <a class="btn btn-light" href="{{ route('admin.gallery.index') }}">Cancel</a></form>@endsection
