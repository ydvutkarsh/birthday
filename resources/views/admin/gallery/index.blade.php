@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">The visual diary</p><h1>Photo <em>gallery</em></h1><p class="muted">Collect the frames that feel like home.</p></div><a class="btn btn-dark-gold" href="{{ route('admin.gallery.create') }}">+ Upload photo</a></div>@include('admin.partials.flash')<div class="studio-card table-card"><div class="row g-3">@forelse($photos as $photo)<div class="col-6 col-md-4 col-xl-3"><div class="admin-gallery-item"><img src="{{ image_url($photo->image) }}" alt="{{ $photo->title }}"><div class="admin-gallery-caption"><strong>{{ $photo->title ?: 'Untitled frame' }}</strong><div><a href="{{ route('admin.gallery.edit',$photo) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.gallery.destroy',$photo) }}" data-confirm="Delete this photo?"><button type="submit">Delete</button>@csrf @method('DELETE')</form></div></div></div></div>@empty<div class="empty-table">No photos yet. Upload the first frame.</div>@endforelse</div></div>@endsection
