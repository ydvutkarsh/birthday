@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">{{ $message->exists ? 'Refine a feeling' : 'A new feeling' }}</p><h1>{{ $message->exists ? 'Edit' : 'Add' }} <em>message</em></h1></div></div>@include('admin.partials.flash')<form method="POST" action="{{ $message->exists ? route('admin.messages.update',$message) : route('admin.messages.store') }}" class="studio-card form-card">@csrf @if($message->exists) @method('PUT') @endif<div class="row g-4"><div class="col-md-6"><label>Title</label><input name="title" value="{{ old('title',$message->title) }}" required><label>Icon <small>one character or emoji</small></label><input name="icon" value="{{ old('icon',$message->icon ?: '✦') }}"></div><div class="col-md-6"><label>Message</label><textarea name="message" rows="5" required>{{ old('message',$message->message) }}</textarea><label>Status</label><select name="status"><option value="published" @selected($message->status !== 'draft')>Published</option><option value="draft" @selected($message->status === 'draft')>Draft</option></select></div></div><label>Display order</label><input type="number" name="sort_order" min="0" value="{{ old('sort_order',$message->sort_order ?? 0) }}"><button class="btn btn-dark-gold mt-4">Save message</button> <a class="btn btn-light" href="{{ route('admin.messages.index') }}">Cancel</a></form>@endsection
