@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">The little things</p><h1>Things I love <em>about you</em></h1><p class="muted">Small truths, written down so they never get missed.</p></div><a class="btn btn-dark-gold" href="{{ route('admin.messages.create') }}">+ Add message</a></div>@include('admin.partials.flash')<div class="studio-card table-card"><div class="table-responsive"><table class="table studio-table"><thead><tr><th>Icon</th><th>Title</th><th>Message</th><th>Status</th><th></th></tr></thead><tbody>@forelse($messages as $message)<tr><td class="message-icon">{{ $message->icon }}</td><td><strong>{{ $message->title }}</strong></td><td>{{ Str::limit($message->message, 70) }}</td><td><span class="status-pill {{ $message->status }}">{{ $message->status }}</span></td><td class="text-end"><a class="action-link" href="{{ route('admin.messages.edit',$message) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.messages.destroy',$message) }}" data-confirm="Delete this message?"><button class="action-link danger" type="submit">Delete</button>@csrf @method('DELETE')</form></td></tr>@empty<tr><td colspan="5" class="empty-table">No messages yet.</td></tr>@endforelse</tbody></table></div></div>@endsection
