@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">{{ $song->exists ? 'Refine the soundtrack' : 'A new song' }}</p><h1>{{ $song->exists ? 'Edit' : 'Add' }} <em>track</em></h1></div></div>@include('admin.partials.flash')<form method="POST" action="{{ $song->exists ? route('admin.music.update',$song) : route('admin.music.store') }}" class="studio-card form-card">@csrf @if($song->exists) @method('PUT') @endif<label>Track title</label><input name="title" value="{{ old('title',$song->title) }}" required><label>YouTube video or playlist URL</label><input type="url" name="youtube_url" value="{{ old('youtube_url',$song->youtube_url) }}" placeholder="https://www.youtube.com/watch?v=…" required><p class="form-hint">Paste a video URL or a playlist URL. The player waits for her first click before starting.</p><label>Display order</label><input type="number" name="sort_order" min="0" value="{{ old('sort_order',$song->sort_order ?? 0) }}"><label class="check-line"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$song->is_active ?? true))> <span>Show this track in the surprise</span></label><button class="btn btn-dark-gold mt-4">Save track</button> <a class="btn btn-light" href="{{ route('admin.music.index') }}">Cancel</a></form>@endsection
