@extends('layouts.admin')
@section('content')<div class="studio-top"><div><p class="eyebrow">The soundtrack</p><h1>Music <em>playlist</em></h1><p class="muted">Add YouTube videos or a full playlist for the moments between the moments.</p></div><a class="btn btn-dark-gold" href="{{ route('admin.music.create') }}">+ Add track</a></div>@include('admin.partials.flash')<div class="studio-card table-card"><div class="table-responsive"><table class="table studio-table"><thead><tr><th>Track</th><th>Source</th><th>Type</th><th>Status</th><th></th></tr></thead><tbody>@forelse($songs as $song)<tr><td><strong>{{ $song->title }}</strong></td><td><a class="text-link" href="{{ $song->youtube_url }}" target="_blank">Open YouTube ↗</a></td><td>{{ $song->youtube_playlist_id ? 'Playlist' : 'Video' }}</td><td><span class="status-pill {{ $song->is_active ? 'published' : 'draft' }}">{{ $song->is_active ? 'active' : 'hidden' }}</span></td><td class="text-end"><a class="action-link" href="{{ route('admin.music.edit',$song) }}">Edit</a><form class="d-inline" method="POST" action="{{ route('admin.music.destroy',$song) }}" data-confirm="Delete this track?"><button class="action-link danger" type="submit">Delete</button>@csrf @method('DELETE')</form></td></tr>@empty<tr><td colspan="5" class="empty-table">No music yet.</td></tr>@endforelse</tbody></table></div></div>@endsection
